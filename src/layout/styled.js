import styled from "styled-components"

export const LayoutContainer = styled.div`
    position:absolute;
    left:0;
    top:0;
    right:0;
    bottom:0;
    padding-bottom:1rem;
    background:#fff
`